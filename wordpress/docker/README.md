# WordPress Docker (LEMP Stack)

This setup runs WordPress using Docker Compose with:

- Nginx
- PHP-FPM (custom image with MySQL extensions)
- MySQL

## How to Run

```bash
docker compose up -d
access: 
Save.

---

## 🟢 STEP 4: Git status check

```bash
git status

