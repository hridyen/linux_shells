# WordPress LEMP Setup (WSL2)

This project automates the installation and management of WordPress
using Linux, Nginx, MySQL, and PHP (LEMP stack).

## Files
- install_wordpress_lemp.sh → Full installation script
- start_wordpress.sh → Start services
- restart_wordpress.sh → Restart services

## Usage
chmod +x *.sh
./install_wordpress_lemp.sh

